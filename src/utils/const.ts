import { createContext } from "react";

export const rectLineColor = "#000000";
export const arcLineColor = "red";
export const maxIterationCount = 35;

export const MaxModContext = createContext<number>(1);